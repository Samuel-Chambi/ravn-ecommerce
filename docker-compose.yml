services:
  localstack:
    image: localstack/localstack:latest
    container_name: ravn-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3
      - DEBUG=1
      - PERSISTENCE=0
    networks:
      - ravn-network

networks:
  ravn-network:
    driver: bridge
